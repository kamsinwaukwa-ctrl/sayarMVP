# Sayar WhatsApp Commerce Platform - Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values

# Backend API Configuration
VITE_API_BASE_URL=http://localhost:8000

# Application Configuration
VITE_APP_NAME=Sayar Dashboard
VITE_APP_VERSION=1.0.0
VITE_APP_DESCRIPTION=WhatsApp Commerce Platform

# Development Settings (optional)
VITE_DEBUG=false
VITE_LOG_LEVEL=info

# Legacy Supabase Configuration (if still needed for realtime features)
# VITE_SUPABASE_URL=your_supabase_project_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Security Notes:
# - These are build-time variables and will be visible in the client bundle
# - Do NOT put sensitive secrets here - use backend environment variables instead
# - All variables must start with VITE_ to be accessible in the frontend
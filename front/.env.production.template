# Sayar Frontend - Production Environment Template
# These variables will be set in Vercel dashboard environment variables

# API Configuration
VITE_API_BASE_URL=https://api.usesayar.com

# Supabase Configuration (Client-side)
VITE_SUPABASE_URL=https://jdtktyvoxsnpiuirwpmr.supabase.co
VITE_SUPABASE_ANON_KEY=[REPLACE_WITH_YOUR_SUPABASE_ANON_KEY]

# Environment
VITE_ENV=production

# NOTE: In production, these values should be set via Vercel environment variables dashboard
# The VITE_ prefix makes these variables available to the frontend build process
# Never commit actual keys to version control!